# 🏟️ شرح مشروع Stadium Gate Manager
## مربوط حرفياً بكورس Ultralytics & Computer Vision

---

## 📌 الفكرة العامة للمشروع

نظام ذكي يراقب بوابات الملعب عبر كاميرا،
يعد الناس، يكشف التدافع، ويعطي المنظم تنبيهات فورية
على داش بورد ويب — كل هذا بـ YOLO + Python + Flask.

---

---

# 🖼️ الوحدة التمهيدية — أساسيات الصور
## "How computers perceive images"

### ما تعلمناه في الكورس:
> الصورة ما هي إلا مصفوفة أرقام (N-Dimensional Array).
> كل بيكسل = 3 أرقام تمثل اللون (R, G, B).
> الإحداثيات تبدأ من الزاوية العلوية اليسرى.

### وين استخدمناه في مشروعنا:

```python
# في stadium_system.py — هذا السطر يقرأ كل فريم من الفيديو
# الفريم = مصفوفة numpy بشكل (H, W, 3) — يعني طول × عرض × 3 ألوان
ret, frame = cap.read()
```

```python
# لما نرسم مستطيل على البوابة
# نحدد نقطتين: (x1,y1) الزاوية العلوية اليسرى و (x2,y2) السفلية اليمنى
cv2.rectangle(frame, (x1, y1), (x2, y2), color, 2)
```

```python
# لما نكشف التدافع نحسب المسافة بين شخصين بالبيكسل
# هذا ممكن لأن الصورة مصفوفة وكل شخص عنده إحداثيات (cx, cy)
dist = np.sqrt((x2 - x1)**2 + (y2 - y1)**2)
```

> ✅ ما تعلمناه في الكورس طبقناه: الصورة = مصفوفة، والإحداثيات = أرقام نتحكم فيها.

---

---

# 🚀 الجزء الأول — Tasks & Inference
## "Discover what YOLO can do out of the box"

---

## 1. Object Detection — كشف الأجسام

### ما تعلمناه في الكورس:
> YOLO يكشف الأجسام ويعطيك Bounding Box لكل جسم.
> الـ Box يتكون من: (x1, y1, x2, y2, confidence, class_id)
> الـ class_id=0 يعني "شخص" في نموذج COCO.

### وين استخدمناه في مشروعنا:

```python
# تحميل النموذج — yolo11n.pt النسخة الصغيرة السريعة
model = YOLO("yolo11n.pt")

# تشغيل الكشف على الفريم
# classes=[0] معناها: اكشف الأشخاص فقط (class 0 = person في COCO)
results = model.track(frame, persist=True, classes=[0], verbose=False)[0]
```

```python
# استخراج إحداثيات كل شخص مكتشف
for box in results.boxes:
    x1, y1, x2, y2 = map(int, box.xyxy[0])  # ← .xyxy شرحه الكورس
    cx, cy = (x1 + x2) // 2, (y1 + y2) // 2  # مركز الشخص
```

> 💡 الكورس شرح إن .xyxy ترجع إحداثيات (top-left, bottom-right)
> وهذا بالضبط ما استخدمناه لتحديد مركز كل شخص.

---

## 2. Object Tracking — تتبع الأشخاص

### ما تعلمناه في الكورس:
> model.track() تعطي كل شخص ID ثابت عبر الفريمات.
> persist=True يعني "تذكر الـ IDs بين الفريمات".
> لو الشخص اختفى وعاد، يحتمل نفس الـ ID.

### وين استخدمناه في مشروعنا:

```python
# استخدمنا track() بدل predict()
# لأننا نبي نتتبع نفس الشخص عبر الفريمات
# persist=True ضروري — بدونه الـ ID يتغير كل فريم
results = model.track(frame, persist=True, classes=[0], verbose=False)[0]
```

```python
# التحقق إن في IDs — لو ما في tracking ما يشتغل الكود
if results.boxes.id is not None:
    for box in results.boxes:
        track_id = int(box.id[0])  # ID فريد لكل شخص
```

> ✅ هذا بالضبط ما شرحه الكورس:
> "Assign distinct IDs to objects and track them continuously across video frames"

---

## 3. Confidence & Classes — الثقة والتصنيف

### ما تعلمناه في الكورس:
> Confidence = مدى ثقة النموذج في اكتشافه (مو ضمان الصح).
> COCO dataset = 80 كلاس جاهزة، الشخص = class 0.

### وين استخدمناه في مشروعنا:

```python
# حددنا classes=[0] = أشخاص فقط
# ما نبي يكشف سيارات أو حقائب، بس أشخاص
model.track(frame, persist=True, classes=[0], verbose=False)

# لو تبي تشوف الـ confidence لكل شخص:
# conf = float(box.conf[0])  # مثلاً 0.87 = 87% ثقة
```

---

---

# 🔍 الجزء الثاني — Use Cases & Solutions
## "Beyond bounding boxes: actionable insights"

هذا الجزء هو **قلب مشروعنا** — كل المنطق الذكي مبني عليه.

---

## 1. Region Analytics — تحليل المناطق

### ما تعلمناه في الكورس:
> تقدر تحدد polygon أو منطقة في الفريم.
> تتحقق هل الجسم داخل هذه المنطقة أو لا.
> هذا أساس كل حلول Ultralytics (Counting, Parking, Zones).

### وين استخدمناه في مشروعنا:

```python
# عرّفنا 4 بوابات، كل بوابة = منطقة مستطيلة (zone)
GATE_DEFINITIONS = {
    "A": {"zone": (50,  100, 320, 520), ...},
    "B": {"zone": (340, 100, 610, 520), ...},
    "C": {"zone": (630, 100, 900, 520), ...},
    "D": {"zone": (920, 100, 1190, 520), ...},
}
```

```python
# دالة تتحقق: هل الشخص داخل منطقة البوابة؟
def point_in_zone(cx, cy, zone):
    x1, y1, x2, y2 = zone
    return x1 < cx < x2 and y1 < cy < y2
    # ← نفس مفهوم الكورس: cross-referencing tracked object coordinates
```

```python
# لكل شخص مكتشف، نشوف في أي بوابة هو
for gate_name, cfg in GATE_DEFINITIONS.items():
    if point_in_zone(cx, cy, cfg["zone"]):
        gate_counts[gate_name] += 1  # عدّ الشخص في هذه البوابة
```

> ✅ الكورس قال:
> "Region analytics rely on defining geospatial boundaries in the frame
> and cross-referencing tracked object coordinates"
> هذا بالضبط ما بنيناه يدوياً لبوابات الملعب.

---

## 2. Object Counting — عد الأشخاص

### ما تعلمناه في الكورس:
> Ultralytics عنده solutions.ObjectCounter جاهز.
> لكن نقدر نبني منطق العد يدوياً لمرونة أكبر.

### وين استخدمناه في مشروعنا:

```python
# عدّاد يدوي لكل بوابة
gate_counts: Dict[str, int] = {g: 0 for g in GATE_DEFINITIONS}

for gate_name, cfg in GATE_DEFINITIONS.items():
    if point_in_zone(cx, cy, cfg["zone"]):
        gate_counts[gate_name] += 1

# تحديث العداد في GateState
gate.count = gate_counts.get(name, 0)
```

```python
# نسبة الامتلاء = العدد الحالي ÷ الطاقة الاستيعابية
@property
def occupancy_pct(self):
    return round((self.count / self.capacity) * 100, 1)
```

---

## 3. Heatmap — خريطة الحرارة

### ما تعلمناه في الكورس:
> Heatmap تُظهر أين يتجمع الناس أكثر.
> مفيدة لتحليل الكثافة والحركة.

### وين استخدمناه في مشروعنا (كشف التدافع):

```python
# بدل Heatmap كامل، استخدمنا مسافة بين الأشخاص
# لو شخصان قريبين جداً (أقل من 45 بكسل) = تدافع محتمل
def detect_crowding_in_gate(positions, threshold=45):
    incidents = 0
    for i in range(len(positions)):
        for j in range(i+1, len(positions)):
            x1, y1 = positions[i]
            x2, y2 = positions[j]
            dist = np.sqrt((x2-x1)**2 + (y2-y1)**2)
            if dist < threshold:
                incidents += 1
    return incidents
```

> 💡 هذا تطبيق عملي لمفهوم Distance Calculation
> اللي شرحه الكورس في قسم "Kinematic and Spatial Measurement"

---

## 4. Security Alarm — التنبيهات الذكية

### ما تعلمناه في الكورس:
> solutions.SecurityAlarm تبعث email لما يُكتشف جسم.
> نقدر نبني نفس المنطق بشكل مخصص.

### وين استخدمناه في مشروعنا:

```python
# محرك القرار — يولّد تنبيهات بناء على حالة البوابة
def _generate_alerts(self):

    # تنبيه تدافع (= SecurityAlarm في الكورس لكن للأشخاص)
    if gate.crowding_incidents > 3:
        gate.alert = f"⚠️ تدافع في بوابة {gate.name}! انتقل فوراً"

    # تنبيه امتلاء مع توجيه للبوابة الأخف
    elif gate.status in ("CRITICAL", "OVERFLOW"):
        gate.alert = (
            f"🔴 بوابة {gate.name} ممتلئة {gate.occupancy_pct:.0f}% "
            f"— وجّه الجمهور لبوابة {lightest_gate.name}"
        )
```

```python
# سجل التنبيهات بالوقت (مثل logs الأمان)
def _log_alert(self, gate, msg, alert_type):
    self.alerts_log.insert(0, {
        "time": time.strftime("%H:%M:%S"),
        "gate": gate,
        "message": msg,
        "type": alert_type,
    })
```

---

## 5. Analytics — الإحصاءات والتوقعات

### ما تعلمناه في الكورس:
> Analytics تحلل البيانات عبر الوقت وترسم trends.

### وين استخدمناه في مشروعنا:

```python
# معدل الدخول (أشخاص/دقيقة) — trend عبر الوقت
def update_entry_rate(self):
    h = list(self.count_history)  # آخر 10 قراءات
    if len(h) >= 2:
        self.entry_rate_per_min = max(0, (h[-1] - h[0]) * 12)

# توقع متى تنتهي عملية الدخول
def get_eta_minutes(self):
    remaining = self.total_expected - total_in
    avg_rate = sum(g.entry_rate_per_min for g in self.gates.values()) or 1
    return round(remaining / avg_rate, 1)
```

---

---

# 📊 الجزء الرابع — Evaluation & Deployment
## "How good is your model and how to use it?"

---

## IoU — تقاطع المناطق

### ما تعلمناه في الكورس:
> IoU = Intersection over Union
> يقيس مدى تداخل منطقتين.
> يُستخدم في تقييم دقة الكشف.

### وين استخدمناه في مشروعنا:

```python
# نفس مفهوم IoU لكن مبسط:
# نشوف هل نقطة مركز الشخص داخل منطقة البوابة
def point_in_zone(cx, cy, zone):
    x1, y1, x2, y2 = zone
    return x1 < cx < x2 and y1 < cy < y2
    # ← بدل IoU الكامل، نستخدم نقطة المركز لأنها أسرع وتكفي
```

---

## Deployment — نشر النموذج

### ما تعلمناه في الكورس:
> بعد التدريب، تصدر النموذج بصيغ محسّنة مثل ONNX.
> يمكن نشره كـ API.

### وين استخدمناه في مشروعنا:

```python
# بنينا Flask API يبعث بيانات النظام بصيغة JSON
# الداش بورد يقرأ منه كل ثانيتين

@app.route("/api/status")
def api_status():
    return jsonify(engine.to_json())

# تشغيل Flask في thread منفصل عن YOLO
flask_thread = threading.Thread(
    target=lambda: app.run(host="0.0.0.0", port=5000),
    daemon=True
)
flask_thread.start()
```

> ✅ هذا نشر حقيقي للنموذج كـ Service يمكن أي جهاز يتصل فيه.

---

---

# 📐 مخطط الربط الكامل

```
الكورس                          مشروعنا
══════════════════════════════════════════════════════
الوحدة التمهيدية                 frame = مصفوفة numpy
(الصور = مصفوفات)               cv2.rectangle, np.sqrt

Part 1: Object Detection         model = YOLO("yolo11n.pt")
(Bounding Boxes)                 results.boxes.xyxy

Part 1: Object Tracking          model.track(persist=True)
(IDs ثابتة)                     box.id[0]

Part 1: Classes & Confidence     classes=[0] = أشخاص فقط
(COCO 80 كلاس)                  

Part 2: Region Analytics         GATE_DEFINITIONS + point_in_zone
(تحليل المناطق)                  

Part 2: Object Counting          gate_counts[gate_name] += 1
(العد في المناطق)                occupancy_pct

Part 2: Heatmap/Distance         detect_crowding_in_gate
(كثافة وتدافع)                  dist < threshold

Part 2: Security Alarm           _generate_alerts()
(تنبيهات ذكية)                   alerts_log

Part 2: Analytics                entry_rate_per_min
(تحليل البيانات)                 get_eta_minutes()

Part 4: Deployment               Flask API
(نشر كـ Service)                http://localhost:5000/api/status
══════════════════════════════════════════════════════
```

---

---

# ▶️ خطوات التشغيل

```bash
# 1. تثبيت المكتبات
pip install ultralytics opencv-python flask flask-cors numpy cvzone

# 2. حط الفيديو في نفس المجلد واسمه stadium.mp4

# 3. شغّل النظام
python stadium_system.py

# 4. افتح dashboard.html في المتصفح
```

**هيكل المجلد:**
```
stadium_project/
├── stadium_system.py   ← شغّله أول
├── dashboard.html      ← افتحه في المتصفح
└── stadium.mp4         ← الفيديو التجريبي
```

